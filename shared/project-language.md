**Write into the issue tracker in English** — titles, bodies, comments. A finding
filed as an issue goes there too. **And write every file this run creates in
the project in English** — a document under `docs/agents/`, a decision record,
a test, a configuration or workflow file, a note. The tracker and the files are
part of the project and outlive this conversation, read by anyone who joins it,
the same way a repository settles on one language and keeps to it. Speak to the
user in their own language; that is a different thing.
