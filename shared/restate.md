**Restate what a subagent hands back in the language the user writes in.** An
agent hands its result back in whatever language it worked in; what is built
from it is read by a person, so it is written in theirs. Assembling is not
quoting.
